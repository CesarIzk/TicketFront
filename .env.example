# URL base de tu API (ticketback). En local, la IP de tu PC en la red
# (no "localhost") si vas a probar desde tu celular con Expo Go —
# localhost dentro del celular es el celular mismo, no tu PC.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
